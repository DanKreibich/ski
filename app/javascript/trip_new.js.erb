
// Testing function. Should be removed
// const testFunction = () => {
//   console.log("Hello from trip_new file");
// };



// Select checkboxes
const bookingSlots = document.querySelectorAll("booking-slot");

const myFunction = () => {
  bookingSlots.classList.toggle("booked")
};

bookingSlots.forEach((element) => {
 element.addEventListener("click", myFunction);
});



addEventListener("click", myFunction);


document.querySelectorAll("img").forEach((img) => {
  img.addEventListener("click", (event) => {
    event.currentTarget.classList.toggle("img-circle");
  });
});

// bookingSlots.addEventListener("click", myFunction);


// const testClick = () => {
//   booking_slots.addEventListener("click", (event) => {
//   console.log("Just clicked the box");
// });
// };





export { myFunction };
